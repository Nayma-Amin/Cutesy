import 'package:flutter/material.dart';

class GraphAnalytics extends StatelessWidget {
  const GraphAnalytics({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}